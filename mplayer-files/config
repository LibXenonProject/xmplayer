# =====================
# Text-based subtitles
# =====================
#<<==Subfont settings==>>
# Settings are only used if ass=no - no outline working atm
# Subfont starting scale
subfont-text-scale=2
# Subfont position (0-100)
subpos=90
#
#<<==ASS/SSA settings==>>
# Disable ASS (ass=no) if you are having random freeze (and #vf=ass)
# ASS Subtitles switch
ass=yes
vf=ass
# Subtitle font style (MarginV is the number of pixels between bottom of screen and sub)
ass-force-style=Outline=1,Shadow=1,FontSize=16,MarginV=5,Alignment=2
#
#<<==General subtitles settings==>>
# Find subtitle files (loads all subs containing movie name)
sub-fuzziness=1
# Font encoding
subfont-encoding=unicode
# Subtitle file encoding
utf8=yes
unicode=yes
